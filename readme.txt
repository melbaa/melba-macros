Vanilla 1.12 addon with random utilities. Does not attempt to be self-contained or user friendly.

Target audience: people learning to make addons.

Mostly uses env.lua (a non-global environment) to manage definitions and internal dependencies.
